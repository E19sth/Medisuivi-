{
  "name": "medisuivi",
  "version": "1.0.0",
  "private": true,
  "description": "MediSuivi - app native Android (Capacitor) avec rappels/alarmes de medicaments",
  "scripts": {
    "build": "esbuild www/app.js --bundle --outfile=www/bundle.js --format=iife --target=es2018"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.23.1"
  }
}
